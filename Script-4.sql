--1) 
SELECT MAJOR AS "학과인"
	 , SNAME AS "학생의 현재 평점은"
	 , AVR AS "입니다."
	FROM STUDENT;
	
--2)
SELECT CNAME AS "과목은"
	 , ST_NUM AS "학점 과목입니다."
	FROM COURSE;
	
--3) 
SELECT PNAME AS "교수는"
	 , SECTION AS "학과 소속입니다."
	FROM PROFESSOR;
	
--4)
SELECT DISTINCT MAJOR
	FROM STUDENT; 
	
--5)
SELECT DISTINCT SECTION
	FROM PROFESSOR;
	
--6)
SELECT DISTINCT ORDERS
	FROM PROFESSOR;